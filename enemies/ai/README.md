IA de enemigos: FSM de 6 estados (idle / roam / alert / chase / attack / search)
como enum + match dentro de GroundedEnemy — el BT de la v1 era prototipo y no se porta.
LimboAI (addon BT) solo si la FSM se queda corta de verdad.
